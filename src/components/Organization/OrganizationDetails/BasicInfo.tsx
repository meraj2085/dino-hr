import React from 'react';

const BasicInfo = () => {
    return (
        <div>
            This is the BasicInfo component from OrganizationDetails
        </div>
    );
};

export default BasicInfo;